# Copy to .env for local testing, or set these in the Render dashboard.
# Without them the site still works: submissions are saved to ./submissions
# and printed to the log instead of being emailed.

SMTP_HOST=smtp.mail.yahoo.com
SMTP_PORT=465
SMTP_USER=gizmozau@yahoo.com
SMTP_PASS=your-app-password-here
MAIL_TO=gizmozau@yahoo.com
# MAIL_FROM=Gizmoz <gizmozau@yahoo.com>
